package dev.yapp.onboarding.yado.core.model

import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.unit.IntSize

data class YadoLocation(
    val offset: Offset,
    val size: IntSize
)